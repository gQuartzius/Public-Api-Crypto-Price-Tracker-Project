<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Select Crypto</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>

<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card">
                    <h1 class="text-center mb-4">Select Crypto</h1>
                    <p class="text-center text-muted mb-4">Multiple cryptos match your search. Choose the correct one:</p>
                    <form action="/select" method="post">
                        <ul class="list-group mb-4">
                            <% cryptos.forEach(function(crypto) { %>
                            <li class="list-group-item">
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="cryptoId" value="<%= crypto.id %>"
                                        id="crypto-<%= crypto.id %>" required>
                                    <label class="form-check-label" for="crypto-<%= crypto.id %>">
                                        <%= crypto.name %> (<%= crypto.symbol.toUpperCase() %>)
                                    </label>
                                </div>
                            </li>
                            <% }); %>
                        </ul>
                        <button type="submit" class="btn btn-primary w-100">Select</button>
                    </form>
                     <a href="/" class="btn btn-secondary w-100 mt-3">Back to Home</a>
                </div>
            </div>
        </div>
    </div>
</body>

</html>