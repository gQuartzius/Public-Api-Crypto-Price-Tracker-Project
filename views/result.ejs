<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Crypto Price Result</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>

<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card">
                    <h1 class="text-center">Crypto Price Result</h1>

                    <% if (price != null) { %>
                        <div class="alert alert-success" role="alert">
                            <p>The price of <span class="fw-bold"><%= cryptoName %></span> is: <span class="fw-bold">$<%= price %></span></p>
                        </div>
                    <% } else if (error) { %>
                        <div class="alert alert-danger" role="alert">
                            <h2 class="alert-heading">Error</h2>
                            <p><%= error %></p>
                        </div>
                    <% } else { %>
                        <div class="alert alert-warning" role="alert">
                            <h2 class="alert-heading">Not Found</h2>
                            <p>No data found for <span class="fw-bold"><%= cryptoName %></span>.</p>
                        </div>
                    <% } %>
                    <a href="/" class="btn btn-primary w-100 mt-3">Check another price</a>
                </div>
            </div>
        </div>
    </div>
</body>

</html>
